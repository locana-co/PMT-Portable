1) copy files to same named folders in PostgreSQL install
2) Need to have PostGIS 2.1 or 2.2+ installed already 

CREATE EXTENSION postgis;
CREATE EXTENSION pointcloud;
CREATE EXTENSION pointcloud_postgis;